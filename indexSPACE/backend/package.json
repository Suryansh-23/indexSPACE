{
  "name": "@indexspace/backend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@functionspace/core": "*",
    "@indexspace/shared": "*",
    "hono": "^4.0.0",
    "viem": "^2.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
